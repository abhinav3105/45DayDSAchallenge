class Solution {
    public String longestCommonPrefix(String[] strs) {
        String s = "";
        StringBuilder sb = new StringBulider(strs[0]);
        for(int i = 1; i< strs.length;i++){
            int k = strs[i].length(); 
            for(int j = 0;j<k;j++){
                if()
            }
        }
                
    }
}
